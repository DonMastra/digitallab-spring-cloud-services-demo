server:
  port: 8099

eureka:
  instance:
    hostname: localhost
  client:
    rsgisterWithEureka: false
    fecthRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/